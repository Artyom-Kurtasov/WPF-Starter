<Window x:Class="WPF_Starter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Starter"
        mc:Ignorable="d"
        Title="DataGridWindow" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Export" Command="{Binding NavigationCommands.ShowExportForm}"/>
            <MenuItem Header="Import csv" Command="{Binding ImportCommands.ImportCommand}"/>
        </Menu>

        <DataGrid x:Name="peoplesGrid"
              Grid.Row="1"
              AutoGenerateColumns="False"
              EnableRowVirtualization="True"
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              ItemsSource="{Binding PagingSettings.GridPeoples}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"
                                Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Surname" Binding="{Binding Surname}" />
                <DataGridTextColumn Header="Patronymic" Binding="{Binding Patronymic}" />
                <DataGridTextColumn Header="City" Binding="{Binding City}" />
                <DataGridTextColumn Header="Country" Binding="{Binding Country}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Назад" Margin="5" Command="{Binding NavigationCommands.GetPreviosPage}"/>
            <TextBlock x:Name="PageIndicator" VerticalAlignment="Center" Margin="5" Text="{Binding PagingSettings.PageIndicator}"/>
            <Button Content="Вперёд" Margin="5" Command="{Binding NavigationCommands.GetNextPage}"/>
        </StackPanel>
        <ProgressBar 
                     x:Name="ProgressBar" HorizontalAlignment="Left" Height="10" 
                     Margin="27,0,0,0" Grid.Row="2" 
                     VerticalAlignment="Center" Width="100"
                     IsIndeterminate="True"
                     Visibility="{Binding LoadingState.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</Window>